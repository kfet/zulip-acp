/bin/
/dist/
*.log
/zulip-acp
.DS_Store
*.local.json
.env
.envrc
# Agent config is per-deployment and never tracked. The skills themselves
# live in ./skills; point an agent at them with a symlink
# (.fir/skills -> ../skills) or a settings entry ("skills": ["skills"]).
/.fir/
*.test

# Agent session scaffolding is never part of this repo.
BRIEF*.md
REPORT*.md
