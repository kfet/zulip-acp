/bin/
/dist/
*.log
/zulip-acp
.DS_Store
*.local.json
.env
.envrc
# Agent config is per-deployment and never tracked. The skills themselves live
# in ./skills; fir finds them via a global settings entry
# ("skills": ["skills"], resolved against the cwd) or a .fir/skills symlink.
/.fir/
*.test

# Agent session scaffolding is never part of this repo.
BRIEF*.md
REPORT*.md

# A real relay config carries ZULIP_API_KEY. The example is the exception.
config.json
!docs/config.example.json
